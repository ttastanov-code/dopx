# requirements-dev.txt
#
# Нагрузочное тестирование (Locust, см. loadtest/locustfile.py) и
# dev-инструменты — НЕ ставятся в прод-образ, см. Dockerfile (он ставит
# только requirements.txt). Django-приложение сам по себе ничего отсюда
# не импортирует — это транзитивные зависимости Locust/Flask-стека и
# отдельные dev-утилиты, которые когда-то попали в общий requirements.txt.
ConfigArgParse==1.7.5
Flask==3.1.3
flask-cors==6.0.2
Flask-Login==0.6.3
geventhttpclient==2.3.9
pdfminer.six==20251230
pdfplumber==0.11.9
playwright==1.58.0
pytest==9.0.2
Werkzeug==3.1.6
